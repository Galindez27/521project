# 521project

## Authors
Jonathan Galindez

## Summary
Currently we can pull *all* the cookies from a firefox session into python and
use them to spoof a python request as a browser with login credentials from the
cookies. There is no session cookie scanning, just a plain session hijack using
all a user's cookies.